mixin news(data)
	+b.news__block
		+e.outer
			+b('span').special-title
					| SHIMKO KITCHEN FURNITURE

			+b.second-heading ПОЛЕЗНАЯ ИНФОРМАЦИЯ

		+e.inner
			+b.news-slider
				if data
					each item in data
						+e.item
							.slider__item-wrapper
								img(src= item.bg alt="news image")
								+e.content
									+e('span').date= item.date
									+e.logo
										img(src= item.img alt= 'logo')
									+e('span').title= item.title
									+e('p').text= item.description

								+b-btnsCommonTransparentDeatails()

			+b-btnsIndividualNews()




mixin projets(data)
	+b.news__block.project__block
		+e.outer
			+b('span').special-title
					| СТИЛЬ МИНИМАЛИЗМ

			+b.second-heading РЕАЛИЗОВАНЫЕ ПРОЕКТЫ

		+e.inner
			+b.news-slider
				if data
					each item in data
						+e.item
							.slider__item-wrapper
								img(src= item.bg alt="news image")
								+e.content
									+e('span').date= item.date