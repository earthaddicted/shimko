extends main

append params
	- page = 'household-page'
	- pageTitle = 'Household appliance'


block main
	block householdParam
		+b.section.breadcrumbs
			+e.wrapper
				+b.breadcrumbs-block
					+e('ul').list
						+e('li').item
							+e('a').link(href="index.html") Главная
						+e('li').item Бытовая техника

		+b.section.section-slider
			+e.wrapper
				+sliderTop(globals.sliderHousehold)

		+b.section.section-sale
			+e.wrapper
				.large-text
					.second-heading При заказе кухни 

						+e('span') скидка до 10%
							br
							| на всю технику и мойки
						br
						|  Постоянные акции

				+b('div').form-submit.btn-popup Подробнее у менеджеров

		+b.section.section-logo
			+e.wrapper
				+logoBlock(globals.logoBlockImage)

		+b.section.two-column.contacts-n__section
			+e.wrapper
				+twoColumn()

		+b.section.map
			+e.wrapper
				+map()
